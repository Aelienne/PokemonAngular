import { ConvertHTtoTTCPipe } from './convert-htto-ttc.pipe';

describe('ConvertHTtoTTCPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertHTtoTTCPipe();
    expect(pipe).toBeTruthy();
  });
});
