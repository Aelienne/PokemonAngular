import { SearchPokedexPipe } from './search-pokedex.pipe';

describe('SearchPokedexPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPokedexPipe();
    expect(pipe).toBeTruthy();
  });
});
